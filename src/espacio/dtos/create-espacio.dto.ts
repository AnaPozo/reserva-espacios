//DTO- data transfer object, el objeto que nos viene en la request y con el que queremos mapear el objeto de la request
//este es el objeto que viene en la request
export class CreateEspacioDTO{
    edificio:string;
    aula:string;
}